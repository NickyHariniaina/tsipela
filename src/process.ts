//      TODO: Complete these functions.

// This will be the main function that will process the text
// correct it and store it in a new file
export function processCorrection(text: string): string {
    return text;
}

// This function will take the
export function processWordFactorization(word: string): string {
    return word;
}
